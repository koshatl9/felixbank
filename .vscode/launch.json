{
    "configurations": [
        {
            "type": "pwa-chrome",
            "name": "FelixBank in Docker",
            "request": "launch",
            "url": "http://127.0.0.1:8000/login/",
            "webRoot": "${workspaceFolder}/frontend",
            "preLaunchTask": "Docker Compose: Up"
        }
    ]
}
